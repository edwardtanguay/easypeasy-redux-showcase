- howto
    - installation
        - use 
        - with "Typed"
    - x message
    - x userLevel 
    - x enable setUserLevel
        - x change with <p>change level: <input value={level} className="text-right w-[4rem] px-1" onChange={(e) => setLevel(Number(e.target.value))}/></p>
        - x also show on another page
            - x page info to profile 
            - x display on profile
        - 
    - x add userName
        - x same as level
    - .. persist data
        - .. persit name only
            -------------------------------------------------------
            export interface MainModel {

            persisted: {
                userName: string;
            }
            userLevel: number;

            persisted: persist (
                {
                    userName: '',
                }, 
                {
                    allow: ['userName']
                }
            ),
            userLevel: 0,
            -------------------------------------------------------